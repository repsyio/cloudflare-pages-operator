# Generated by Fabric8 CRDGenerator, manual edits might get overwritten!
apiVersion: "apiextensions.k8s.io/v1"
kind: "CustomResourceDefinition"
metadata:
  name: "cloudflarepages.pages.repsy.io"
spec:
  group: "pages.repsy.io"
  names:
    kind: "CloudflarePage"
    plural: "cloudflarepages"
    shortNames:
    - "cfpage"
    singular: "cloudflarepage"
  scope: "Namespaced"
  versions:
  - additionalPrinterColumns:
    - jsonPath: ".metadata.creationTimestamp"
      name: "Age"
      priority: 0
      type: "date"
    - description: "Custom domain for the site. When its zone is in the operator's\
        \ Cloudflare account the CNAME record is managed automatically."
      jsonPath: ".spec.domain"
      name: "Domain"
      priority: 0
      type: "string"
    - description: "Container image that contains the built static site."
      jsonPath: ".spec.image"
      name: "Image"
      priority: 1
      type: "string"
    - jsonPath: ".status.message"
      name: "Message"
      priority: 1
      type: "string"
    - jsonPath: ".status.phase"
      name: "Phase"
      priority: 0
      type: "string"
    - jsonPath: ".status.url"
      name: "URL"
      priority: 0
      type: "string"
    name: "v1alpha1"
    schema:
      openAPIV3Schema:
        properties:
          spec:
            properties:
              branch:
                default: "main"
                description: "Production branch name of the Pages project."
                type: "string"
              directory:
                description: "Absolute path of the directory inside the image to publish."
                pattern: "^/.+$"
                type: "string"
              domain:
                description: "Custom domain for the site. When its zone is in the\
                  \ operator's Cloudflare account the CNAME record is managed automatically."
                maxLength: 253
                pattern: "^([a-z0-9]([-a-z0-9]{0,61}[a-z0-9])?\\.)+[a-z]{2,63}$"
                type: "string"
              image:
                description: "Container image that contains the built static site."
                minLength: 1
                type: "string"
              projectName:
                description: "Cloudflare Pages project name. Defaults to <namespace>-<name>.\
                  \ Immutable."
                pattern: "^[a-z0-9]([-a-z0-9]{0,56}[a-z0-9])?$"
                type: "string"
              revision:
                description: "Change this value to force a redeploy of the same image,\
                  \ e.g. when using a mutable tag."
                type: "string"
            required:
            - "directory"
            - "domain"
            - "image"
            type: "object"
            x-kubernetes-validations:
            - message: "projectName is immutable"
              rule: "has(self.projectName) == has(oldSelf.projectName) && (!has(self.projectName)\
                \ || self.projectName == oldSelf.projectName)"
          status:
            properties:
              conditions:
                items:
                  properties:
                    lastTransitionTime:
                      type: "string"
                    message:
                      type: "string"
                    observedGeneration:
                      type: "integer"
                    reason:
                      type: "string"
                    status:
                      type: "string"
                    type:
                      type: "string"
                  type: "object"
                type: "array"
              deployJob:
                type: "string"
              deployedAt:
                type: "string"
              deployedHash:
                type: "string"
              deployedImage:
                type: "string"
              dnsRecordId:
                type: "string"
              domain:
                type: "string"
              domainStatus:
                type: "string"
              failedHash:
                type: "string"
              message:
                type: "string"
              observedGeneration:
                type: "integer"
              pagesDevUrl:
                type: "string"
              phase:
                type: "string"
              projectCreated:
                type: "boolean"
              projectName:
                type: "string"
              url:
                type: "string"
              zoneId:
                type: "string"
            type: "object"
        type: "object"
    served: true
    storage: true
    subresources:
      status: {}
